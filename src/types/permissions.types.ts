/**
 * Shared Permission Types
 *
 * Re-exports the Permission type from lib/auth/permissions.ts.
 * Import from here in feature types to avoid circular deps.
 */

export type { Permission } from "@/lib/auth/permissions";
